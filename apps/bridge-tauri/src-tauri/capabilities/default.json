{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core IPC + the external-URL opener (allowlist enforced in Rust).",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
