node_modules/
dist/
build/
out/
*.tsbuildinfo
.env
.env.local
.DS_Store

# Local Tally probe captures — may contain real financial data
/scratch/
*.tally-capture.txt

# Visual harness output. Regenerated by `npx electron scripts/visual-harness.mjs`; PNGs are build
# artefacts, not source. They also render fixture data through the real card layer, so a future
# harness pointed at a real keystore would put real figures in them — keep them out of git.
.visual/

# The deploy bundle: generated by `npm run build:deploy-bundle` from apps/server + packages.
apps/bridge/deploy-bundle/

# Ruflo / claude-flow runtime state (regenerated; not project source)
.swarm/
.claude-flow/
.hive-mind/
